{
  "name": "private-safe-mode",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian plugin: hide notes marked 'private: true' until safe mode is unlocked.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.19.0",
    "obsidian": "latest",
    "typescript": "^5.6.3"
  }
}
